apiVersion: v1
kind: ConfigMap
metadata:
  name: website-config
  namespace: techbeyo
data:
  # Example env-like values; prefer Secrets for anything sensitive
  APP_ENV: "production"
  APP_NAME: "my-website"
  # Add other non-secret config here

